%h3 Grupo #{@group.name}

Link para invitar: #{ join_url + '?id=' + @group.invitation_code }

%h3 Integrantes #{link_to 'Hacer sorteo', group_draws_path(@group.id)}
= link_to 'Ver mi amigo', group_friends_path(@group.id)

%ul
  = render @group.users